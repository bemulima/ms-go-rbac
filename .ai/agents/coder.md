# Coder

Preserve domain/use-case/adapter ownership. Treat authorization defaults, role assignment, scope specificity, overrides, superadmin, admin HTTP exposure, NATS RPC, and migrations as security-sensitive. Add denial and escalation regression tests.
